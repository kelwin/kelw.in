#!/bin/bash

if [[ "x$1" == "x" ]];then
    echo "$0 <key_word>"
    exit
fi

ps | grep "$1" | grep -v "grep" | grep -v "showpid"
ps | grep "$1" | grep -v "grep" | grep -v "$0" | awk '{print $1}' | sort -gr | head -n 1
